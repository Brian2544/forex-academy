import 'package:flutter/material.dart';

class SignalsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Trading Signals')),
      body: Center(
        child: Text('Signals Screen - Coming Soon'),
      ),
    );
  }
}

